# ============================================================
# scholar preset · 星炬物理学霸（学习引擎）
# 组合：纯对话学习角色——不暴露 code agent 工具（bash/fs/goal/jobs 移除）。
# persona 由 @aemeath/dsh-plugin-common 按 preset 挂载；
# worldbook 注入 scholar 馆；讲义检索/解题工作流由对应插件提供。
# ============================================================
- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536
