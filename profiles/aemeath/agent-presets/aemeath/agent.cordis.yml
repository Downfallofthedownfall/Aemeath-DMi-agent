# ============================================================
# aemeath preset · 爱弥斯桌宠（陪伴/情感/日常）
# 组合：纯对话角色——不暴露任何 code agent 工具（bash/fs/goal/jobs 全部移除）。
# persona 由 @aemeath/dsh-plugin-common 按 preset 挂载；
# worldbook 注入 aemeath 馆；检索工具被 restrict 隐藏。
# ============================================================
- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536
